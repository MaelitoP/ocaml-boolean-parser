(library
 (name boolean_parser)
 (public_name ocaml-boolean-parser)
 (preprocess
  (pps ppx_deriving.eq ppx_deriving.show)))

(ocamllex lexer)

(menhir
 (modules tokens)
 (flags --only-tokens)
 (explain false))

(menhir
 (modules tokens parser)
 (merge_into parser)
 (flags --external-tokens Tokens))
